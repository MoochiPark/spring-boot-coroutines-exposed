spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  exposed:
    excluded-packages: io.wisoft.springexposed.configure

spring.exposed.generate-ddl: true
